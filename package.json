{
  "name": "job-scheduler",
  "version": "1.0.0",
  "private": true,
  "description": "Distributed Job Scheduling Platform — Monorepo",
  "scripts": {
    "start:api": "npm --prefix backend run start",
    "start:worker": "npm --prefix backend run start:worker",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "test": "npm --prefix backend run test"
  },
  "keywords": ["job-scheduler", "distributed-systems", "queue"],
  "license": "MIT"
}
